server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/clubhub?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
    username: ariven
    password: Ariven#2026Aa!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB

sa-token:
  token-name: jmi-openatom
  timeout: 2592000
  active-timeout: -1
  is-concurrent: false
  is-share: false
  token-style: random-32
  is-log: false

app:
  upload-dir: uploads
  default-password: 123456
